include $(makefiles)/rules.mk

ifeq ($(cfg__install_guile_lib),1)
include $(makefiles)/install-scm-subdirs.mk
endif

ifneq ($(cfg__guile_lib_install_dir),default)
scm-install-dir := $(destdir)$(cfg__guile_lib_install_dir)
else
scm-install-dir := $(destdir)$(prefix)/share/guile/site
endif

# arch-tag: 1aefadd1-89a2-4a39-9872-75ec828c976d
